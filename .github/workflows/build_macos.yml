name: Build macOS Client

on:
  push:
    branches: [ "main", "master", "dev" ]
  pull_request:
    branches: [ "main", "master", "dev" ]
  workflow_dispatch:

jobs:
  build:
    name: Build macOS App
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install Qt
      uses: jurplel/install-qt-action@v3
      with:
        version: '6.7.0'
        host: 'mac'
        target: 'desktop'
        modules: 'qtremoteobjects qt5compat'

    - name: Install build tools
      run: |
        brew install cmake

    - name: Build XRay Core (Stub)
      # In a real scenario we'd build XRay from source or download a prebuilt binary.
      # For now we create a dummy binary to satisfy build requirements if needed, 
      # or rely on the client to download it at runtime.
      run: |
        mkdir -p client/bin
        touch client/bin/xray-core
        chmod +x client/bin/xray-core

    - name: Configure CMake
      run: |
        mkdir build
        cd build
        cmake .. -DCMAKE_BUILD_TYPE=Release -DCMAKE_PREFIX_PATH=$Qt6_DIR

    - name: Build
      run: |
        cd build
        cmake --build . --config Release

    - name: Create DMG
      run: |
        $Qt6_DIR/bin/macdeployqt build/client/AmneziaVPN.app -dmg -verbose=1

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: AmneziaVPN-macOS-DMG
        path: build/client/*.dmg
        if-no-files-found: warn
